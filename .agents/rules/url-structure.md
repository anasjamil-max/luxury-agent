# Folder-Based Clean URL Page Structure

All pages in this repository must be structured using subdirectories containing `index.html`:

```text
page-name/
└── index.html
```

Do not create root `.html` files (like `about.html` or `services.html`) or rely on rewrite configs/JS URL manipulation.
